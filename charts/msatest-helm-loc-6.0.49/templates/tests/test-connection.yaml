apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "msatest-helm-loc.fullname" . }}-test-connection"
  labels:
    {{- include "msatest-helm-loc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "msatest-helm-loc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
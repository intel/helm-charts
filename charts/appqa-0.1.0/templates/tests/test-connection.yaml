apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appqa.fullname" . }}-test-connection"
  labels:
    {{- include "appqa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appqa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

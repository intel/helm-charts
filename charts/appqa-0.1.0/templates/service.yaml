apiVersion: v1
kind: Service
metadata:
  name: {{ include "appqa.fullname" . }}
  labels:
    {{- include "appqa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appqa.selectorLabels" . | nindent 4 }}
